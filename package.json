{
  "name": "diagram-to-grist",
  "version": "0.1.0",
  "description": "The utility script to read a diagram data and import it to the Grist database.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Denis Zhelnerovich",
  "license": "ISC",
  "dependencies": {
    "@maxgraph/core": "^0.2.0",
    "fast-xml-parser": "^4.2.2",
    "node-fetch": "^3.3.1"
  },
  "devDependencies": {
    "@babel/plugin-syntax-import-assertions": "^7.20.0"
  }
}
